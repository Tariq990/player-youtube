[pytest]
minversion = 7.0
addopts = -ra --cov=src --cov-report=term-missing --cov-report=html --cov-fail-under=100
testpaths = tests
pythonpath =
    src
markers =
    e2e: end-to-end tests
    slow: slow tests
